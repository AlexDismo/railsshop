<!DOCTYPE html>
<html>
  <head>
    <title>Shoptest</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= javascript_include_tag "turbo", type: "module" %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

  </head>

  <body>
    <header class="w-screen">
      <div class="flex m-auto w-4/5 justify-between items-center py-5 border-b">
        <div>
          TestShop
        </div>
        <nav clas='flex gap-5'>
          <span class="btn hover:underline px-10"><%= link_to "Main", home_path %></span>
          <span class="btn hover:underline px-10"><%= link_to "Shop", products_path %></span>
          <span class="btn hover:underline px-10"><%= link_to "Account", edit_user_registration_path, method: :delete, data: { turbolinks: false } %></span>
          <% if current_user && current_user.admin? %>
            <span class="btn hover:underline px-10"><%= link_to "Users", users_path %></span>
          <% end %>
          <span class="btn hover:underline px-10"><%= link_to "Cart", carts_path %></span>

        </nav>
        <button class="border p-2 hover:bg-gray-200">
          <% if user_signed_in? %>
            <%= link_to "Logout", destroy_user_session_path, method: :delete, data: { turbolinks: false } %>
        <% else %>
            <%= link_to "Login", new_user_session_path %> / <%= link_to "Sign up", new_user_registration_path %>
          <% end %>
        </button>
      </div>
    </header>
    <main class="container mx-auto mt-28 px-5 flex">
      <%= yield %>
    </main>
  </body>
</html>
